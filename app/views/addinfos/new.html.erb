<h1><%= t('helpers.submit.create', :model => "Informacion Adicional") %></h1>
<%= form_for([@parent, @child]) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.create') %>
  </div>
<% end %>
