<h1><%= t('helpers.submit.update', :model => "Direccion") %></h1>
<%= form_for([@parent, @address]) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.edit') %>
  </div>
<% end %>
