<div class="page-toolbar mb-3">
  <h1 class="h2 mb-0"><%= t('helpers.submit.create', :model => "Archivos Adjuntos") %></h1>
</div>

<div class="card border-0 shadow-sm app-form-card subrecord-form-card">
  <div class="card-body p-3 p-md-4">
    <%= form_for([@parent, @child], html: { class: "subrecord-form app-form" }) do |f| %>
      <%= render 'fields', :f => f %>
      <div class="d-flex justify-content-end gap-2 mt-3">
        <%= f.submit t('helpers.create'), class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>
