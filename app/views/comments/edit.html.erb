<h1><%= t('helpers.submit.update', :model => "Comentario") %></h1>
<%= form_for([@patient, @comment]) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.edit') %>
  </div>
<% end %>
