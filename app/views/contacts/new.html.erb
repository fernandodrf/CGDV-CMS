<h1><%= t('helpers.submit.create', :model => Contact.to_s) %></h1>
<%= nested_form_for(@contact) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.create') %>
  </div>
<% end %>
