<div class="page-toolbar mb-3">
  <div class="d-flex flex-column flex-lg-row justify-content-between align-items-start align-items-lg-center gap-2">
    <h1 class="h2 mb-0"><%= @contact.name %></h1>
    <%= link_to t('helpers.submit.update', :model => "Contacto"), edit_contact_path(@contact), class: "btn btn-primary btn-sm" %>
  </div>
</div>
<%= render 'contactinfo' %>
<%= render 'addinfos/additionalinformation', :model => @contact  %>
<%= render 'telephones/telephoneinformation', :model => @contact %>
<%= render 'addresses/addressinformation', :model => @contact %>
<%= render 'emails/emailinfo', :model => @contact  %>
<%= render 'comments/comments', :model => @contact  %>
