<h1><%= t('helpers.submit.update', :model => "Horario por Dia") %></h1>
<%= form_for([@volunteer, @dailyschedule]) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.edit') %>
  </div>
<% end %>
