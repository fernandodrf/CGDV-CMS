<h1><%= t('helpers.submit.create', :model => "Derechohabiente") %></h1>
<%= form_for([@patient, @seguro]) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.create') %>
  </div>
<% end %>
