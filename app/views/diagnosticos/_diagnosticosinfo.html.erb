<div class="card border-0 shadow-sm detail-section-card diagnosticos">
  <div class="card-header d-flex justify-content-between align-items-center gap-2">
    <strong><%= t('patient.diagnosticos') %></strong>
    <%= link_to t('helpers.submit.create', :model => "Diagnostico"), new_polymorphic_path([model, Diagnostico]), class: "btn btn-outline-primary btn-sm" %>
  </div>
  <div class="table-responsive">
    <table class="table mb-0 app-table">
      <tbody>
        <% unless model.diagnosticos.empty? %>
          <%= render @diagnosticos, :model => model %>
        <% else %>
          <tr><td class="text-muted">Sin diagnósticos.</td></tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
