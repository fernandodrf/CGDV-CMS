<h1><%= t('helpers.submit.update', :model => "Diagnostico") %></h1>
<%= form_for([@patient, @diagnostico]) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.edit') %>
  </div>
<% end %>
