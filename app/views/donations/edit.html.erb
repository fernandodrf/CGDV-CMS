<h1><%= t('helpers.submit.update', :model => Donation.to_s) %></h1>
<%= form_for(@donation) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.edit') %>
  </div>
<% end %>
