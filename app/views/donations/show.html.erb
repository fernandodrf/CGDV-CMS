<div class="page-toolbar mb-3">
  <div class="d-flex flex-column flex-lg-row justify-content-between align-items-start align-items-lg-center gap-2">
    <h1 class="h2 mb-0"><%= t('donation2.title') %>: <%= @donation.folio %></h1>
    <%= link_to t('helpers.submit.update', :model => "Donación"), edit_donation_path(@donation), class: "btn btn-primary btn-sm" %>
  </div>
</div>

<div class="card border-0 shadow-sm">
  <div class="card-body p-0">
    <div class="table-responsive">
      <table class="table mb-0 app-table">
        <tbody>
          <tr>
            <th class="table-light w-25"><%= t('patient.cgdvcode') %></th>
            <td><%= @donation.donor.cgdvcode %></td>
          </tr>
          <tr>
            <th class="table-light"><%= t('patient.name') %></th>
            <td><%= @donation.donor.name %></td>
          </tr>
          <tr>
            <th class="table-light"><%= t('donation2.frecepcion') %></th>
            <td><%= l @donation.frecepcion, :format => :long %></td>
          </tr>
          <tr>
            <th class="table-light">Motivo</th>
            <td><%= donation_motivo(@donation.motivo) %></td>
          </tr>
          <tr>
            <th class="table-light">Tipo</th>
            <td><%= donation_tipo(@donation.tipo) %></td>
          </tr>
          <tr>
            <th class="table-light">Transaccion</th>
            <td><%= @donation.transaccion %></td>
          </tr>
          <tr>
            <th class="table-light">Monto</th>
            <td><%= @donation.monto %></td>
          </tr>
          <tr>
            <th class="table-light">Finalidad</th>
            <td><%= @donation.finalidad %></td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</div>
