<div class="page-toolbar mb-3">
  <div class="d-flex flex-column flex-lg-row justify-content-between align-items-start align-items-lg-center gap-2">
    <h1 class="h2 mb-0"><%= @donor.name %></h1>
    <%= link_to t('helpers.submit.update', :model => "Donador"), edit_donor_path(@donor), class: "btn btn-primary btn-sm" %>
  </div>
</div>
<%= render 'donorinfo' %>
<%= render 'telephones/telephoneinformation', :model => @donor %>
<%= render 'addresses/addressinformation', :model => @donor %>
<%= render 'emails/emailinfo', :model => @donor  %>
<%= render 'addinfos/additionalinformation', :model => @donor %>
<%= render 'comments/comments', :model => @donor %>
