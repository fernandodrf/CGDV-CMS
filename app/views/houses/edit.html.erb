<h1><%= t('helpers.submit.update', :model => "Datos Habitacion") %></h1>
<%= form_for([@patient, @house]) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.edit') %>
  </div>
<% end %>
