<% secondary_links = capture { render 'layouts/links2' } %>
<% if secondary_links.strip.present? %>
  <nav class="card border-0 shadow-sm app-section-nav app-nav-row app-nav-row-top" aria-label="Secondary navigation">
    <div class="card-body py-2">
      <ul class="nav nav-pills nav-fill justify-content-start flex-wrap gap-2">
        <%= secondary_links %>
      </ul>
    </div>
  </nav>
<% end %>
