<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <%= render 'layouts/stylesheets' %>
    <%= javascript_include_tag 'jquery', 'jquery-ui', 'autocomplete-rails', 'jquery.textareaCounter.plugin', 'nested_form', 'rails', 'application' %>
    <%= csrf_meta_tag %>
    <%= render 'layouts/ga' if Rails.env.production? %>
  </head>
  <body>
    <div class="container">
      <%= render 'layouts/header' %>
      <div class="navbar round">
        <%= render 'layouts/navigation' %>
      </div>
      <div class="navbar round">
        <%= render 'layouts/navigation2' %>
      </div>  
      <section class="round">
      	<% flash.each do |key, value| %>
          <div class="flash <%= key %>"><%= value %></div>
        <% end %>
        <div class="main">
		  <%= yield %>
		</div>
      </section>
      
      <%= render 'layouts/footer' %>
      <%= render 'layouts/footer2' %>
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>
