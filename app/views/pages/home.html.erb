<h1><%= t('home.title') %></h1>
<% if signed_in? %>
  <p>
  </p>	
<% else %>
  <p>
    <%= t('home.paragraph') %>
  </p>

  <%= link_to t('session.login'), new_user_session_path, :class => "signup_button round" %>
<% end %>