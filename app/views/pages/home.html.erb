<div class="page-toolbar mb-3">
  <h1 class="h2 mb-0"><%= t('home.title') %></h1>
</div>

<div class="card border-0 shadow-sm home-hero-card">
  <div class="card-body p-3 p-md-4">
    <% if signed_in? %>
      <div class="d-flex flex-column flex-lg-row justify-content-between align-items-start gap-3">
        <div>
          <h2 class="h4 mb-2">Bienvenido</h2>
          <p class="mb-0 text-muted">
            Usa la navegación superior para acceder a pacientes, notas, voluntarios y reportes.
          </p>
        </div>
      </div>
    <% else %>
      <p class="mb-3">
        <%= t('home.paragraph') %>
      </p>
      <%= link_to t('session.login'), new_user_session_path, class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
