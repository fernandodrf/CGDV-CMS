<table class="patientphones">
  <tr><td><strong><%= t('patient.tels') %></strong></td>
	<td><strong><%= link_to t('helpers.submit.create', :model => "Telefono"), new_patient_patientphone_path(@patient) %></strong></td></tr>
  <% unless @patient.patientphones.empty? %>
  <%= render @patientphones %>
  <% end %>
</table>
<% unless @patient.patientphones.empty? %>
  <strong><%= link_to t('helpers.submit.create', :model => "Telefono"), new_patient_patientphone_path(@patient) %></strong><br />
<% end %>