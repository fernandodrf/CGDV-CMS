<h1><%= t('helpers.submit.create', :model => "Telephone") %></h1>
<%= form_for([@patient, @phone]) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.create') %>
  </div>
<% end %>
