<h1><%= t('helpers.submit.update', :model => Patient.to_s) %></h1>
<%= form_for(@patient) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.edit') %>
  </div>
<% end %>
