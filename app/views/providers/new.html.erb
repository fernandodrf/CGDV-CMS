<h1><%= t('helpers.submit.create', :model => "Proveedor") %></h1>
<%= nested_form_for(@provider) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.create') %>
  </div>
<% end %>
