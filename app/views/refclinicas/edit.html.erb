<h1><%= t('helpers.submit.update', :model => "Referencia Clinica") %></h1>
<%= form_for([@patient, @refclinica]) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.edit') %>
  </div>
<% end %>
