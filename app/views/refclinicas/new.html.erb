<h1><%= t('helpers.submit.create', :model => "Referencia Clinica") %></h1>
<%= form_for([@patient, @refclinica]) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.create') %>
  </div>
<% end %>
