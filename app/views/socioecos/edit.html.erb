<h1><%= t('helpers.submit.update', :model => "Informacion Socioeconomica") %></h1>
<%= form_for([@patient, @socioeco]) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.edit') %>
  </div>
<% end %>
