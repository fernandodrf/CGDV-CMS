<h1><%= t('helpers.submit.update', :model => Timereport.to_s) %></h1>
<%= nested_form_for(@timereport) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.edit') %>
  </div>
<% end %>
