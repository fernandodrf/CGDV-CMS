<h1><%= t('helpers.submit.update', :model => User.to_s) %></h1>

<%= form_for @user, :html => {:multipart => true} do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.edit') %>
  </div>
<% end %>