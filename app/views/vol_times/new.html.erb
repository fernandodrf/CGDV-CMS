<h1><%= t('helpers.submit.create', :model => "Tiempo Pendiente") %></h1>
<%= nested_form_for(@voltime) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.create') %>
  </div>
<% end %>
