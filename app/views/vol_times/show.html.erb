<div class="page-toolbar mb-3">
  <div class="d-flex flex-column flex-lg-row justify-content-between align-items-start align-items-lg-center gap-2">
    <h1 class="h2 mb-0"><%= @voltime.volunteer.name %></h1>
    <div class="d-flex flex-wrap gap-2">
      <%= link_to t('helpers.submit.update', :model => "Reporte de Tiempo"), edit_vol_time_path(@voltime), class: "btn btn-primary btn-sm" %>
      <% if can? :read, VolTime %>
        <%= link_to "Regresar a Añadir/Acumular Tiempo", vol_times_path, class: "btn btn-outline-secondary btn-sm" %>
      <% end %>
    </div>
  </div>
</div>

<div class="card border-0 shadow-sm">
  <div class="card-body p-0">
    <div class="table-responsive">
      <table class="table mb-0 app-table">
        <tbody>
          <tr><th class="table-light w-25"><%= t('patient.cgdvcode') %></th><td><%= @voltime.volunteer.cgdvcode %></td></tr>
          <tr><th class="table-light"><%= t('volunteer.created_at') %></th><td><%= l @voltime.created_at %></td></tr>
          <tr><th class="table-light"><%= t('volunteer.evento') %></th><td><%= @voltime.evento %></td></tr>
          <tr><th class="table-light"><%= t('volunteer.horas') %></th><td><%= @voltime.horas %></td></tr>
        </tbody>
      </table>
    </div>
  </div>
</div>
