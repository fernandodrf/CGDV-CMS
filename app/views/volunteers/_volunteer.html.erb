<tr>
  <td><%= link_to "#{volunteer.name}", volunteer %></td>
  <td><%= link_to "#{volunteer.cgdvcode}", volunteer %></td>
  <td><%= link_to volunteer.tt, trep_volunteer_path(volunteer) %></td>
  <% if can? :destroy, @volunteer %>
  <td><%= link_to t('helpers.delete.msg'), volunteer, :method => :delete, :confirm => t('helpers.delete.conf'), :title => "#{t('helpers.delete.msg')} #{volunteer.name}" %></td>
  <% end %>
</tr>
