<h1><%= t('helpers.submit.create', :model => "Voluntario") %></h1>
<%= nested_form_for(@volunteer) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit t('helpers.create') %>
  </div>
<% end %>
